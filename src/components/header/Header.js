import React from 'react';
import "./Header.css";
import Logo from "../logo/Logo";

function Header(props) {
    return (
        <header>
            <Logo />
        </header>
    );
}

export default Header;